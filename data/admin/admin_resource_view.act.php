<?php
  $resource = Resource::instance( getValue('id') );

  if(!is_null(getValue('action'))) {
    switch( getValue('action') ) {
       default:
        break;
    }
  }
  
  // CUSTOM

  //Custom content

  // /CUSTOM
