<?php
  $world = World::instance( getValue('id') );

  if(!is_null(getValue('action'))) {
    switch( getValue('action') ) {
       default:
        break;
    }
  }
  
  // CUSTOM

  //Custom content

  // /CUSTOM
