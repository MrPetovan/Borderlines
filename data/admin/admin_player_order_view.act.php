<?php
  $player_order = Player_Order::instance( getValue('id') );

  if(!is_null(getValue('action'))) {
    switch( getValue('action') ) {
       default:
        break;
    }
  }
  
  // CUSTOM

  //Custom content

  // /CUSTOM
